#include <iostream>
#include <vector>
using namespace std; 
class Node{
    public:
    int data;
    Node* right;
    Node* left;
};
Node* createnewnode(int newdata){
    Node* temp = new Node();
    temp->data=newdata;
    temp->
}
int main(){
    
    return 0;
}
